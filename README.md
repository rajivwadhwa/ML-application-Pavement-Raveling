# ML-application-Pavement-Raveling

In this project, we have pre-processed the range image data of pavement raveling.
We have trained our ML model to detect and classify the pavement raveling severity
